config.properites.example :
    db.url
    db.user
    db.password

For the test :
Copy/paste all lines in database.sql and inserts.sql

ENDPOINTS:
    GET/transactions?type=in|out
    GET/accounts/{id}/transactions
    POST/transactions
    GET/account/{id}/balance